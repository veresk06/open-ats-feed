node_modules
.DS_Store

# Intermediate snapshots from ad-hoc runs — not part of the published measurement.
data/tokens-sample-src.json
data/sample-coverage.json
data/feed.ndjson
data/tokens-before-s3.json
data/tokens-final.json
data/tokens-gh-403.json
data/coverage-final.json

# Working logs from our own runs. The reproducible artifacts are the data/ summaries.
verify-*.log
data-harvest*.log

# Actor local run state
actor/storage

# transient probe logs
lever-resume.log
